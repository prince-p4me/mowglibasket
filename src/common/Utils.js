export let clickable = true

export const log = (tag, value) => {
    // console.log(tag + " ==>  " + value)
}

export const alertStockLimit = (value) => {
    alert("Available quantity in stock is "+value)
}